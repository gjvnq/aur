# Generated by debtap
# Maintainer: libopenusb
# Contributor: libopenusb
pkgname=libopenusb-deb
pkgver=1.1.16
pkgrel=1
pkgdesc="Provides a library for application access to USB devices. Development libs and headers are in openusb-devel (Converted from a deb package)"
arch=('i686' 'x86_64')
url=""
license=('LGPL')
groups=('')
provides=("libopenusb=${pkgver}")
conflicts=('libopenusb')
depends=('libudev0')
options=('!strip' '!emptydirs')
install=${pkgname}.install
source_x86_64=("https://ufpr.dl.sourceforge.net/project/openusb/libopenusb/libopenusb-1.1.16/libopenusb_1.1.16-0_amd64.deb")
sha512sums_x86_64=('3bacc9a7bde8c8fe10eadd61032bb48119cd46d5d2ac77f643916d1ccaa0f0982e6fc8eed5a8e629f395d1423fb8af975c8a33b595686dddd2ea470db8856ae5')

package(){

	# Extract package data
	tar xzf data.tar.gz -C "${pkgdir}"

	cd "${pkgdir}"
	install -D -m644 "usr/share/doc/packages/libopenusb/LICENSE" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
	install -D -m644 "usr/share/doc/libopenusb/copyright" "${pkgdir}/usr/share/licenses/${pkgname}/COPYRIGHT"

}
